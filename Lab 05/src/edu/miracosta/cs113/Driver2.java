//Driver for programming exercises 1 and 2 section 2.2 on page 71
package edu.miracosta.cs113;

public class Driver2 
{
	public static void main(String[] args)
	{
		//the book didn't provide a class to test with, and I ran out of time I hope this is ok!
	}
	/** Add an entry to theDirectory or change an existing entry.
	 * @param aName The name of the person being added or changed
	 * @param newNumber The new number to be assigned
	 * @return The old number, or if a new entry, null
	 */
	
	/**
	public static String addOrChangeEntry(String aName, String newNumber)
	{
		for(Entry person : ArrayList)
		{
			if(person.getName().equalsIgnoreCase(aName))
			{
				String oldNumber = person.getNumber();
				person.setNumber(newNumber);
				return oldNumber;
			}
		}
	}
	*/
	
	/** Remove an entry.
	 * @param aName The name of the person being removed
	 * @return The entry removed, or null if there is no entry for aName
	 */
	
	/**
	public static Entry removeEntry(String aName)
	{
		for(Entry person : ArrayList)
		{
			if(person.getName().equalsIgnoreCase(aName))
			{
				ArrayList.remove(aName);
			}
		}
	}
	*/
}
